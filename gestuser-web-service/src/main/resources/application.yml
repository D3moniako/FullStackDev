#==========================================================
#= GestUser Web Service - Versione Base
#==========================================================

server:
  port: 8019
spring:
  application:
    name: GestUser
#==========================================================
#= PARAMETRI DB MONGODB

 # c'è l'autentication db che è admin, use admin poi ora
  #uso mongo -u admin --authenticationDatabase admin -p root e mi autentico come admin nome admin
  # ora creo user condb.createUser({
  #    user:"pippo",
  #    pwd:"D3mondante.@",
  #    roles:[{ role: "readWrite", db: "ms-users"  }]
  #})
  #poi ora switcho mettendo nome e pass di questo utente
  #mongo -u pippo--authenticationDatabase admin -p D3mondante.@
  #ora  che sono nel nuovo utente uso
  # use ms-users per spostarmi nel db del nuovo utente
  # qui o creo le mie query o permetto tramite file yml a spring di farlo!!!
#==========================================================
  data:
    mongodb:
      authentication-database: admin
      database: ms-users
      host: localhost
      password: D3mondante.@
      port: 37017
      username: pippo
      auto-index-creation: true
